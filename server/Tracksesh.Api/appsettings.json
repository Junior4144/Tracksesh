{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "//": "Real values belong in user-secrets, environment variables, or appsettings.Development.json \u2014 not here. See the 'Configuring the API half' section of the root README.md.",
  "ConnectionStrings": {
    "Postgres": ""
  },
  "Supabase": {
    "Url": "",
    "PublishableKey": "",
    "//JwtSecret": "Only for the local `supabase start` stack, which still signs with a shared HS256 secret. Leave unset against a hosted project so tokens are verified against its published JWKS instead."
  },
  "Cors": {
    "//": "Empty by default: `vite dev` proxies /api to this process, so the browser sees one origin and no CORS is involved.",
    "AllowedOrigins": []
  },
  "Security": {
    "//": "Both matter only outside Development.",
    "//TrustProxyHeaders": "Set true when a TLS-terminating proxy sits in front. Without it every request looks like plain HTTP, so HSTS is never sent and redirection would loop. With it, X-Forwarded-Proto is authoritative and app-level redirection is skipped because the proxy owns it. Leave false when this process holds its own certificate.",
    "TrustProxyHeaders": false,
    "//HstsDays": "A browser that sees the header refuses plain HTTP for this long, and it cannot be called back. Start at 30; raise to 365 once TLS has been boring for a while.",
    "HstsDays": 30
  }
}
